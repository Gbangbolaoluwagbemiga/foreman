node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# runtime registrations (contains wallet addresses)
data/

# local MCP config (machine path + wallet)
.mcp.json

# internal implementation plan — never pushed
PLAN.md

# publishable package's local install
packages/*/node_modules/
