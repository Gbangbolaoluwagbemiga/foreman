export default function RunPage() {
  return (
    <div className="py-16">
      <h1 className="text-2xl font-semibold">Run a job</h1>
      <p className="mt-2 text-muted">
        Give the Foreman a goal and a budget, then watch it hire and pay a crew live — settled in
        real USDC on Arc.
      </p>
      <div className="mt-8 rounded-xl border border-edge bg-panel p-8 text-sm text-muted">
        Hooking up the live run + Gateway settlement next.
      </div>
    </div>
  );
}
